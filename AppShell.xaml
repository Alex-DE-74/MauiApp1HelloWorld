<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="KidJumpUp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:KidJumpUp"
    xmlns:pages="clr-namespace:KidJumpUp.Pages"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Übungen"
        ContentTemplate="{DataTemplate pages:ExercisesPage}"
        Route="ExercisesPage">

        <ShellContent.ToolbarItems>
            <ToolbarItem
                Text="+ Übung"
                Command="{Binding Source={RelativeSource AncestorType={x:Type Shell}}, Path=BindingContext.AddExerciseCommand}" />
        </ShellContent.ToolbarItems>

    </ShellContent>
</Shell>
